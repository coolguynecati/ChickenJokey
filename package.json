{
  "name": "emikashotchicken-repo",
  "private": true,
  "scripts": {
    "postinstall": "npm install --omit=dev --prefix shop",
    "start": "npm start --prefix shop",
    "install:all": "npm install --prefix shop"
  }
}
