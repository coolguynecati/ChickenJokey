services:
  - type: web
    name: emikashotchicken-shop
    runtime: node
    rootDir: shop
    plan: free
    buildCommand: npm install && node scripts/copy-images.js
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "22"
